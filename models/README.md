// SPDX-License-Identifier: Apache-2.0
// Copyright 2026 Hematite Contributors.

# models/ — TFLite model zoo for hematite-codegen tests

## sine.tflite

- **Provenance**: Generated by `tools/generate_sine_model.py` using the Python
  `flatbuffers` library (pip `flatbuffers` 25.12.19) against the vendored
  schema at `tools/tflite-schema/schema.fbs` (tensorflow v2.14.1, flatbuffers
  v23.1-era). The tflite-micro pinned SHA `18b9e6f2a8c5a9518e588f59c2ba16ef7ef9d551`
  has no checked-in int8 sine model (only `python/tflite_micro/sine_float.tflite`,
  float) and no local TensorFlow environment exists — see the schema provenance
  header for the fallback chain.
- **Architecture**: Single-element INT8 input → FullyConnected (1×1 weight, 1 bias) → INT8 output
- **Weight**: 51 (int8, Q7); Bias: -3 (int32)
- **Quantization**: input scale=0.1, output scale=0.1, weight scale=0.0078125 (1/128)
- **Purpose**: Minimal valid TFLite model for testing the flatbuffer parser (T4.0).
- **SHA256**: `fd4b7d2f5e67dbede3909dd4f7178575404168b2b88f14f464361266714f053f`
- **Regenerate**: `python3 tools/generate_sine_model.py models/sine.tflite` (deterministic; output is byte-identical across runs).
