[package]
name = "hematite-ref"
version.workspace = true
edition.workspace = true
repository.workspace = true
keywords.workspace = true
categories.workspace = true
license.workspace = true
readme = "README.md"
description = "Scalar reference backend (RefBackend) — the bit-exact golden oracle for Hematite's SIMD kernels."

[dependencies]
hematite-core = { path = "../hematite-core" }
hematite-int8 = { path = "../hematite-int8" }
hematite-memory = { path = "../hematite-memory" }

[dev-dependencies]
hematite-tests = { path = "../hematite-tests" }
