[package]
name = "hematite-memory"
version.workspace = true
edition.workspace = true
repository.workspace = true
keywords.workspace = true
categories.workspace = true
license.workspace = true
readme = "README.md"
description = "Compile-time USMP-style liveness arena planner (ArenaPlan, ScratchLayout) for zero-allocation inference."

# Zero external dependencies — this is a compile-time planning crate.
# Codegen (T4.2b) imports OpInfo and calls liveness_plan().
[dependencies]
