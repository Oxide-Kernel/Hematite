[package]
name = "espnn-codegen"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Emit ESP-NN baseline C harness for the zoo tflite models (Phase 3 benchmark tooling)"

[dependencies]
hematite-tflite = { path = "../../hematite-tflite" }
hematite-memory = { path = "../../hematite-memory" }
hematite-core = { path = "../../hematite-core" }
hematite-ref = { path = "../../hematite-ref" }
